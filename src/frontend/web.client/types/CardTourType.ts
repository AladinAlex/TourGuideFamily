
export type CardTourType = {
    image: string,
    name: string,
    minParticipants: number,
    maxParticipants: number,
    price: number,
    durationHour: number | undefined,
    dayCount: number,
    slug: string
}